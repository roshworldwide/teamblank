[package]
name = "sentinelwipe-desktop"
version = "0.1.0"
edition = "2021"
description = "SENTINELWIPE desktop shell — the approach film and the instrument, offline"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
strip = true
lto = true
codegen-units = 1

[workspace]
