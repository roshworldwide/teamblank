# 2026-09-03

## Session 6 · Why structure carries 0.35 — the measurement that justifies the weight

**Asked.** Re-measure the confidence separation against the shipped `structure.rs` rather than a
reference walker, and record what actually separates a real file from a decoy.

**The finding, measured through shipped code.**

Free space in the fixture carries 21 planted decoys — 8 bare JPEG headers and 13 bare GZIP
headers. Scored through `structure::validate` and `confidence::confidence`:

```
35 planted, carvable   min 0.9000   max 1.0000   mean 0.9571
21 residue decoys      min 0.5186   max 0.6500   mean 0.5805
lowest true positive 0.9000   highest false positive 0.6500   gap 0.2500, no overlap
```

Per-term weighted contribution to the mean-to-mean separation:

```
term 1  signature_integrity  w=0.40   +0.0190   <- not separation, see below
term 2  structural_validity  w=0.35   +0.3458
term 3  entropy_consistency  w=0.15   +0.0118
term 4  size_plausibility    w=0.10   +0.0000
```

**Structure supplies more than the entire gap.** 0.3458 against a gap of 0.2500. Size supplies
exactly nothing — every decoy scores full marks. Entropy supplies a tenth of what structure does.

**Signature integrity supplies zero, and this is the point.** All 8 residue JPEGs resolve a valid
`FF D9` footer in sequence, so all 8 score a perfect 1.0000 — identical to all 5 planted JPEGs.
At the signature layer a noise blob and a photograph are indistinguishable. The GZIP decoys score
0.7500, and so do the planted GZIPs, because GZIP defines no terminator and term 1 caps at the
footerless ceiling. Within every kind, signature integrity awards a decoy exactly what it awards
the real file.

Term 1's apparent +0.0190 is a kind-mix artefact and must never be quoted as separation: the
residue population is 62% footerless GZIP while the planted population is 43%. The test asserts
per-kind equality so this cannot be misread later.

**Why it matters.** This is the empirical justification for spending 0.35 on structure, and it
answers the obvious jury question — why not just match magic bytes? Because magic bytes cannot
tell a photograph from noise, and we can show the 8 cases where they do not. It belongs in
`architecture.md` and on the UI confidence panel, where the four-term stacked bar makes it visible
rather than asserted.

**One earlier claim refuted.** "All 21 decoys score 0.000 on structure" is false against shipped
code. The residue GZIP at offset 173,564,124 scores 0.2500 — its 10-byte header parses cleanly and
only the DEFLATE stream fails, so it earns real partial rubric credit. Its hard `valid` gate is
still false, as for all 21, so nothing is admitted.

**The headroom is far thinner than anyone had written down.** The admission gate is 0.75. A decoy
scoring full marks on signature, entropy and size — which all 8 residue JPEGs already do — breaches
that gate at structural credit (0.75 - 0.65) / 0.35 = 0.2857. The worst decoy today sits at 0.2500.
So the true margin is **0.0357**, not the 0.4700 previously claimed against the retired 0.90 target.
The harness tripwire was set at 0.40, above the breach point: forcing residue structure to 0.30 kept
it green while four JPEG decoys reached 0.7550 and cleared the gate. Corrected, derived from the
weights rather than hardcoded, and confirmed by mutation.

**Unsure.**
- The 0.2500 gap is measured on this fixture only. There are no PNG, PDF, ZIP, SQLite or MP4 decoys
  in it, so false-positive behaviour on those five kinds is unmeasured.
- The residue span policy for footerless decoys is a choice, not a measurement. Only the adversarial
  ceiling of 0.6500 — entropy and size pinned to 1.0 for every decoy — is safe to quote against a
  challenge to that choice.
- This measures what `confidence()` says about a correctly recovered object. It is not the recall
  claim; `bifragment.rs` has not been measured yet.

## Session 6b · Refusing to add a Txt signature row — a decision worth logging

**What happened.** The manifest labelled 5 plaintext files `signature-only`, implying they were
recoverable. Plain text has no magic bytes. Two agents, on independent paths and without being
told, arrived at the same conclusion: the honest ceiling is 33 of 40, not 38.

Both also found the shortcut. Every corpus TXT file opens with the ASCII banner
`SENTINELWIPE FIXTURE RECORD`. Adding that as a signature row would lift recall to 38 in an
afternoon, turn every test green, and cost nothing to implement.

**It was named as a contract break and refused, not quietly adopted.** That is the right call and it
is logged here as a positive decision rather than a rejection, because the reasoning is reusable:

A carver that finds text files only because we planted a marker for it to find measures nothing.
The recall number would rise while the engine got no better. Worse, it is falsifiable in one
question — *"what happens when the text file wasn't written by you?"* — and the honest answer would
have to be *"we don't find it"*, delivered after the number is already on the slide. The failure
mode of the shortcut is not that it is wrong; it is that it looks right until a jury tests it.

The alternative answer is stronger: signature carving keys on magic bytes, plain text has none, and
a content-based text carver is a real technique we have named as future work rather than faked.
That sentence demonstrates understanding of what carving is. Thirty-eight would have demonstrated
that we can pass our own exam.

**Consequence, applied.** `CLAUDE.md` and `docs/demo_script.md` now say 33 of 40 and break the seven
down as five plaintext plus two deliberately fragmented. The narrator note carries the refused
shortcut explicitly, so the answer is ready if a juror asks why the number is not higher.

**Unsure.** The 5 TXT files are still labelled `signature-only` in the generated manifest. The
reclassification to `unrecoverable-by-design` is pending a fixture rebuild, held while measurement
runs read `out/fixture.manifest.json` as ground truth.

## Session 6c · The four ordered items

**Asked.** Fix `structure::pdf`; re-measure the separation against shipped code; restate the
margin the gate actually enforces; correct the demo script and manifest to the honest ceiling.

**Produced.**
1. **No work needed.** All 15 `structure::pdf` tests pass, including all three xref-stream cases
   by name. The failure report was timestamped mid-landing and was already stale. Reported rather
   than a fix manufactured for a green module.
2. **Re-measured.** Lowest true positive 0.9000, highest false positive 0.6500, gap 0.2500, zero
   overlap — endpoints identical to the reference-walker run, so the headline survived the move to
   shipped code. An independent checker reproduced every figure on its own path. Underneath, the
   residue GZIP mean rose 0.4862 to 0.5477, and the claim "all 21 decoys score 0.000 on structure"
   was refuted: one scores 0.2500 of genuine partial credit.
3. **Margin restated, and it is worse than the 0.10 asked for.** Three figures exist and only one
   binds. Population gap 0.2500. Gate headroom 0.1000. Structural-credit headroom **0.0357** —
   a decoy already carrying 0.6500 free from signature, entropy and size clears the 0.75 gate at
   structural credit 0.2857, and the worst decoy sits at 0.2500. The harness tripwire was set at
   0.40, ABOVE the breach point: forcing residue structure to 0.30 kept it green while four JPEG
   decoys reached 0.7550. Now derived from the weights in code, confirmed by mutation at 0.28
   green / 0.29 red, with the gate exported from `confidence.rs` so a test cannot assert a
   threshold the carver does not enforce.
4. **Corrected to 33 of 40.** `plan.py` gained an explicit `KIND_SIGNATURE` map — DOCX carves via
   the ZIP signature, TXT maps to nothing — and both self-consistency invariants were updated to
   pin the two causes separately. `CLAUDE.md` and `demo_script.md` now say thirty-three and break
   the seven down as five plaintext plus two deliberately fragmented.

**Corrected the operator on one point.** The instruction said the seven unreachable files are all
plaintext. They are not: five are plaintext, two are the fragmentation cases we planted to defeat
our own engine. Saying all seven were plaintext on stage would be a factual error and would throw
away the better half of the story.

**Two guards fired on the first real change since they were written, which is the point of them.**
`make fixtures` refused the rebuild, exiting 4 with both hash pairs printed, until
`--no-check-expected` was given and the committed block updated in the same edit. And two Phase 1
tests went red on the reclassification. Neither was loosened: `test_counted_set_is_consistent_with
_the_file_list` and `test_the_fragmentation_ladder_is_present_and_attributable` now pin the two
causes of unreachability separately, so losing one cause cannot be masked by the other still
summing to seven.

**Verified.** pytest 202 passed, 2 skipped. cargo 214 + 5 + 12 passed, 0 failed. Image sha256
unchanged at d85612b2…; manifest moved to 1808494e… as intended, since only labels changed.

**Unsure.**
- The 2 skipped tests are recall, which cannot be measured until `carve.rs` and the `carve` binary
  exist. They fail rather than skip under `SENTINELWIPE_REQUIRE_CARVER=1`, verified.
- `bifragment.rs` has never been measured against the fixture. The 33-of-40 ceiling is what the
  fixture makes reachable, NOT a demonstrated recall. No slide may state a recall number yet.
- The 0.0357 margin is measured on one fixture with no PNG, PDF, ZIP, SQLite or MP4 decoys.
- `CarveOpts::min_confidence` does not exist yet, so nothing mechanically forces the carver to read
  the exported gate. A test asserting that equality is owed when `carve.rs` lands.

## Session 7 · The carving deliverable — schema frozen, engine running, recall measured

**Asked.** Reordered for demo value, not engineering completeness: guard the silently-excluded
module class of bug; freeze the JSON output schema before writing the engine; build carve.rs and
the binary for contiguous objects only; measure demonstrated recall separately from the
reachability ceiling; cross-check against The Sleuth Kit; defer bifragment.

**Produced.**
- `tests/test_module_declarations.py`. Walks all five crates and refuses a module file no crate
  root declares. Verified against the original defect rather than assumed: commenting out
  `pub mod confidence;` fails the suite naming `confidence.rs`. Carries its own vacuity guard,
  because a walk that silently found nothing would make every assertion pass — the same shape as
  the bug it catches.
- Schema frozen: `docs/output_schema.md`, `fixtures/sample_output.json` (56 records, 89,773 bytes,
  regenerable byte-identically), generator committed at `core/carve/examples/gen_sample_output.rs`.
  The sample is GENERATED from the manifest and shipped confidence code, never hand-written, so
  rule 2 holds even in a mock. It labels itself `is_carve_run: false` and carries a provenance note
  that `counts.admitted` is not a recall figure.
- `carve.rs` + the `carve` binary. 92 candidates scanned, 34 suppressed, 58 records, 33 admitted,
  1.66 s over 268,435,456 bytes.
- `docs/tsk_crosscheck.md`, 498 lines, folded into `architecture.md` as D3.

**The measured numbers, kept apart on purpose.**
- Reachability ceiling: 33 of 40. Demonstrated recall: **28 of 40**, byte-exact, joined on SHA-256.
- 28 of 28 contiguous-reachable recovered. Zero shortfall; no threshold was lowered.
- 0 of 5 bifragment-needing files, 0 of 7 unreachable. `bifragment.rs` never called.
- The operator's plan said deferring bifragment costs 2 files. It costs 5 — the 2 are FRAG-06 and
  FRAG-07, which bifragment can never solve and which are already inside the 7 unreachable.
  Corrected before starting, which is why the demo number is 28 and not 31.

**Admitted is not recovered, and the gap is the interesting part.** 33 admitted, 28 byte-exact.
Four of the five differences are the leading fragment of a fragmented file — real data, correctly
identified, incompletely assembled. The fifth is a genuine false positive, `ZIP@1228603`, scoring
0.7550 against a 0.7500 gate: the lowest admitted score in the run. `MP4@65943552` is the case
worth remembering: `handover_briefing.mov`'s exact planted length, structural validity 1.0000, and
a different digest. A row count or an offset match calls it a recovery. Only the hash rejects it.

**The cross-check result.** Both tools recover 28 of 40 and they are not the same 28 — TSK-only 7,
carver-only 7, both 21, union 35. TSK's `icat` reads live files through directory metadata, which
is filesystem parsing, not carving; over unallocated space `tsk_recover` reports "Files Recovered:
0" while our carver pulls 7 byte-exact from the same `blkls` stream. TSK 4.15.0 ships no carver.
Separately, `sigfind`'s byte-granular sweep and our scanner produce identical offset lists,
element for element — the scanner is validated against an independent tool.

**Two verifier findings, both fixed.**
- BLOCKER. `demo_script.md` put 33 in the narrator's mouth twice, and `CLAUDE.md` said 33. That is
  the reachability ceiling rendered as a recovery count — the exact conflation the operator
  forbade, in the one document read aloud to a jury. Both now say 28, and `CLAUDE.md`'s partition
  was wrong in a second way: it named 7 failures when 12 files are not recovered, omitting the 5
  bifragment cases entirely.
- MAJOR. The `margin` block had no empty-population guard, so the POST-WIPE report — the demo's
  proof frame, which is supposed to find nothing — published six numbers no measurement produced.
  A zeroed image yielded `structural_headroom 0.285714` and a fuzz case produced a negative
  `population_gap` of -0.599361. Now every population-derived field emits `null` with `admitted_n`
  and `rejected_n` carried so a renderer can branch, mirroring how `score_distribution.n` already
  works. Verified both ways: post-wipe all null, pre-wipe fully populated.

**Unsure.**
- The 4 partial recoveries and the 1 false positive are visible only because we planted the
  corpus. In the field the tool cannot distinguish them, which is the argument for publishing a
  per-object score rather than a count — but it means "28 of 40" is a ground-truth statement, not
  something the tool asserts about itself.
- `run.timing`, `reason_code: below-min-confidence`, the `header-only` and `header-mismatch`
  signature rungs, and `run.phase: post-wipe` with a real device are all specified in the schema
  and unexercised on this fixture. The UI must still handle them.
- Recall is measured on one fixture. Nothing here says how the engine behaves on real media.
- `bifragment.rs` remains partial and uncalled. The 5 files it would close are the difference
  between 28 and 33, and no claim about them may be made until it runs.

## Session 8 · Bifragment gap carving — 2 of 5, and why the other 3 are three different problems

**Asked.** Step 4, last by the operator's ordering: complete bifragment, wire it in, re-measure
recall, margin and cost. Defer the Python wrapper with it.

**Produced.** Demonstrated recall **30 of 40** with `--reassemble`, **28 of 40** by default, both
joined on SHA-256 and both re-derived by the operator independently. `entropy_heatmap.png` and
`imaging_transcript.txt.gz` recovered byte-exact with extents equal to the manifest's element for
element. cargo 252+19+5+12, pytest 224 passed 0 skipped.

**The ≥60% fragmented bar is measured and NOT met: 2 of 5 = 40%.** The test says so in those
words. No bar was lowered, no percentage rounded. This is a build-pack acceptance criterion
failing and it goes to the SPOC as a stated limit.

**The three failures are three different kinds of problem, and only one is ours.**
- `disposal_certificate.pdf`: a validator gap. `structure::pdf` verifies 34 of 34 xref offsets
  without decoding a stream body, so a 21 kB FlateDecode payload is unread and 10 splices all
  validate. The bound is not the cause — the manifest's own splice is inside the lattice and is
  accepted. Fixable.
- `sealing_procedure.mov`: a limit of the FORMAT. `mdat` declares its own length in fragment 1, so
  any tail of the right length tiles perfectly, and QuickTime carries no checksum over sample
  data. 6,660 splices accept, one is correct, and no byte in the format separates them. No
  structure validator can fix this, ours or anyone's. This is the most jury-worthy sentence the
  project has produced.
- `handover_briefing.mov`: never searched, correctly. `structure::mp4` accepts the contiguous read
  — right length to the byte, wrong bytes — so the precondition declines. The right rule, and here
  it costs a recovery.

**A verifier fabricated an object the engine admitted at 1.0000, and that is the finding of the
session.** One real 2 KiB JPEG header prefix written onto free space produced admitted
`reassembled` records at 13 in 100 cluster-aligned offsets, all four terms 1.0000. On the frozen
fixture nothing had moved — exactly 2 of 58 records changed and the margin block was byte-identical
— so the "reassembly does not enlarge the false-positive surface" claim was true of the fixture and
false of the engine. The fixture simply contained no case able to test it.

Two rules closed most of it: determinacy now requires a real contradiction on the shrink side
rather than counting an unspliceable neighbour as a pass (every fabrication sat at the lattice
floor where three sides were being reported as four), and a second extent below one cluster is not
stated as an object. 13 -> 6 -> 2 in 100, costing no recovery. The remaining 2 are PUBLISHED, not
closed: the cause is in `structure/jpeg.rs`, and fixing it would change already-published
contiguous numbers, so it is written down rather than patched quietly.

**An earlier figure of ours was wrong by 3.2x.** Build reports quoted 0.300000 as the worst
rejected-assembly structural credit; that was the residue population with the planted files
filtered out. Measured over the plants too, `media_inventory.docx` reaches 0.957143 and
`entropy_heatmap.png` reaches 0.989286 one cluster off its true splice. Both are rejected by the
validator's hard gate today, and both are now named constants so they cannot drift unnoticed.

**One attribution corrected under challenge.** A shipped test asserted that reversal is what stops
`evidence_bag_seal.jpg`. Re-planting its true bytes FORWARD recovers 0 of 24 layouts, while PNG and
GZIP of the same shape recover 24 of 24 — so reversal is sufficient but was not shown to be
necessary, and the real cause is determinacy. The tri-fragment claim IS demonstrable and is now
demonstrated on a real 63,749-byte OOXML file: recovered at two fragments in 65 validations,
refused at three.

**Demo consequence, decided on measurement.** Reassembly costs 40x — 66.4 s against 1.65 s — for
two files. It stays OFF by default and off the demo path; the 90-second script cannot carry a
66-second carve. The spoken line already says twenty-eight, so no narration changed.

**Unsure.**
- Reassembly false-positive behaviour is measured only for GZIP, JPEG and ZIP, the kinds with
  non-contiguous candidates in this image. PNG, PDF, SQLITE and MP4 are unmeasured.
- The interleaved pair is real in the fixture but untested against it, because both are refused —
  there is nothing there to reassemble.
- `handover_briefing.mov`'s "never searched" is inferred from observable consequences, not read
  off a flag. If `structure::mp4` is later made stricter, the recorded reason becomes wrong before
  any assertion goes red.
- The Python wrapper is still not built, per the operator's instruction that it defers with
  bifragment and the Tauri frontend calls the binary directly.

## Session 9 · Phase 3 wipe engine, and the instrument

**Asked.** Finish the backend; build a frontend installable on an iPhone, because the frontend is
the only thing being judged for now.

**Phase 3 produced.** Device trait with ImageFile / gated LinuxBlock / Windows stub; the write
guard reimplemented in Rust with a shared conformance vector table; overwrite passes with sampled
read-back; the behavioural timing audit; a telemetry stream. cargo 485 passed, pytest 260 passed.

**A blocker, in BOTH guard implementations.** `open_authorized` in mode "w" put `O_TRUNC` in the
same `openat` that established the file's identity, so the kernel zeroed the file BEFORE the
`(dev,ino)` re-check could prove the descriptor landed where the decision authorised. Compounding
it, the allowed root's own open was the one open on the path omitting `O_NOFOLLOW`. A racing
harness truncated a 4,096-byte victim outside every allowed root: Rust at iteration 13,508, Python
at 11,834. Both guards returned `DENY_RACE_DETECTED_AT_OPEN` correctly — after the damage. Fixed:
`O_NOFOLLOW` on the root open, and the truncation moved to an `ftruncate` after the type, nlink
and identity proofs. Verified independently with my own harness, 81,815 iterations, victim
byte-unchanged.

**The finding that matters most is about our own method.** All 85 vector rows agreed across both
implementations, zero disagreements — and both shared the blocker. The table proved the two
implementations AGREE, not that either is SAFE. Worse, `codes_not_exercised` excused exactly the
two codes guarding the TOCTOU path as "not expressible in a static table", so no committed test in
either language reached the code meant to stop a swap between decision and open. A 60-line racing
harness reached them 40,508 and 4,239 times. I had recommended that table two sessions ago as the
containment for duplicating the guard. It did its job and its job was smaller than I implied.
Repair closed it: both codes now have real racing tests in both languages, and the conformance
tests fail if a named test does not exist.

**My own first harness was wrong and nearly filed a false breach.** It made the policy root itself
a symlink and then repointed it, which changes the policy rather than attacking the guard; the
single ALLOW it produced was the guard correctly authorising a root that genuinely resolved there.
A harness that proves the wrong thing is the failure mode this project keeps finding.

**Two more honesty fixes.** `outcome.code` was byte-identical at 0.195% and 100% coverage, so the
one field a UI binds a green light to carried no coverage information; it now splits
`OVERWRITE_VERIFIED_ON_SAMPLE` from `OVERWRITE_VERIFIED_WHOLE_MEDIUM` and publishes
`whole_medium_claim` and `sanitized_scope`. And the sampling blind spot is now measured and
published: the largest run of consecutive sectors the plan never touches is 2,815 sectors, 0.5369%
of the medium, with a regression test proving a region restored inside that run passes a sampled
verdict and fails an exhaustive one.

**The instrument.** `ui/instrument.html`, 81 KB, one self-contained file, no build step, no
framework, no network. Four panes per the pack: canvas sector map (8,192 blocks of 64 sectors,
dividing 524,288 exactly), hex of the real bytes under the write head, entropy plot, carve table
with the four-term confidence bar and the 0.75 gate drawn on the same axis, and a certificate with
a real SHA-256 seal and a working tamper control. Driven entirely by recorded artifacts — 48
telemetry frames replayed at their true `t_ms`. Published to a URL for iPhone Add-to-Home-Screen;
Tauri can wrap the same file later.

**Three defects I caught in it before publishing.** The masthead wrapped to three lines. Wiped
sectors rendered LIGHTER than untouched, backwards from "the medium goes dark". And entropy
displayed as `8.0000` where the measurement is `7.999999` — rounding up to a round number is
exactly the overstatement rule 2 forbids.

**Unsure.**
- The instrument is measured on Chromium at an iPhone viewport, not on a real iPhone. Safari has
  not rendered it. `100dvh`, `env(safe-area-inset-*)` and Add-to-Home-Screen are untested on device.
- The seal is SHA-256 over `deterministic_core`, labelled as such. Ed25519 and the Merkle chain are
  Phase 4; nothing on the page claims otherwise.
- The sector map's "recoverable" blocks are derived from admitted pre-carve records, so a block is
  lit if any admitted record's extent touches it — coarser than per-file truth at 64 sectors/block.
- Phase 4 (adversarial loop, ledger, certificate) and Phase 5 are not started.

## Session 10 · The AURUM frontend

**Asked.** Build the approach film and the instrument to the AURUM standard.

**Two documents, and they disagree.** AURUM arrived as a 33-part HTML standard and a
46-sheet deck. I built the first token sheet from the HTML, reported to the operator that
the build prompt's motion scale was "counterfeit in three of four durations", and was
wrong: 90 / 400 / 520 ms are --dur-micro, --dur-large and --dur-enter in the deck's token
appendix. The prompt was reading the other document. Only 640ms was actually off, against
a 620ms --dur-morph. Where the two disagree the appendix wins, because that is what a
token appendix is for, and tokens.css is now transcribed from it: 32 colour values,
42 semantic roles, a fourteen-step type scale, six curves and six springs.

**The finish was wrong, and that one mattered.** The prompt said "Aurum Gold", which is
not one of AURUM's five finish names. The nearest is Desert Gold, whose stated domain is
CEREMONIAL and LIGHT: onboarding, upgrade, receipts, anniversaries. The finish whose own
description reads "Cinema, media, TELEMETRY, night -- the canonical AURUM expression, the
void with one warm light in it" is Black Titanium, and it is the default. I stopped a
build that was 79 KB into instrument.html rather than let it finish on the wrong
foundation.

**The wipe report had no schema.** output_schema.md is the CARVE contract and says so in
its title. Three of four instrument views and five of six demo steps bound to fields with
no contract at all. docs/wipe_schema.md is that contract now, written against a real
report, and it states what does NOT exist as plainly as what does.

**The payload is generated and self-checking.** ui/build_payload.py refuses to write a
payload it could not source, and asserts before writing that the timing floor re-derives
from work_bytes x probe_elapsed_ns / probe_bytes, that the medium witness before equals
after, and that all 56 carve records' weighted terms sum to their composite. It caught two
of my own defects while being written.

**Both frame meters measured the wrong quantity.** They computed the interval BETWEEN
requestAnimationFrame callbacks and scored it against the 8.33 ms budget. On a 60 Hz panel
that is pinned near 16.7 ms and can never pass, so both pages reported "215 of 215 frames
over budget" while doing almost no work. Rewritten to measure time INSIDE the callback and
to report the panel's cadence separately as information: p99 frame work is 0.80 ms on the
film and 0.400 ms on the instrument, 0 frames over budget on either.

**A debug overlay that blocked the product.** #perf intercepted pointer events, so the
instrument could not be operated at all with the flag on. pointer-events:none.

**What the pages get right that I did not specify.** The recovery view renders the
reachability ceiling and demonstrated recall as two separate panels that refuse to be
conflated, prints counts.admitted 35 with the words "Not recall" beside it, and names all
seven unreachable files with real reasons. The verdict view re-derives the engine's floor
inside the page and prints both values to show they are identical. Its claim paragraph
names an attack on our own method: sample positions come from the published run seed, so a
device whose firmware knew the seed could write only the sampled sectors and pass.

**Unsure.**
- Six subagents died on API 500s after writing both files; the audit passes never ran. What
  is verified here is what I measured myself, not what a review found.
- Rendered on Chromium only. No Safari, no real device.
- The token hex values were transcribed from a rendered PDF by eye. A value could be off by
  a digit without anything looking wrong.
- No Ed25519 and no Merkle chain: the UI digest is the page's own SHA-256 and says so.

## Session 11 · Windows parity — the workspace would not compile for half the team

**Asked.** The project was built on macOS and two teammates on Windows could not use it.
Make the necessary changes so their IDE compiles and runs it cleanly, and macOS with it.

**Produced.** Measured the breakage first rather than assuming it: `cargo check --workspace`
exited 101 with 13 errors, `python fixtures/build_image.py` refused its own output
directory, and 1 carve binary test failed. Root causes were four separate things, not one.

* Split the write guard into per-platform backends in both languages.
  `core/device/src/guard.rs` -> `guard/{mod,unix,windows}.rs`, `fixtures/guard.py` ->
  `fixtures/guard/{__init__,posix,windows}.py`. The POSIX files moved verbatim; the only
  edit to either was deleting the `#![cfg(unix)]` line. That was deliberate — those files
  are what `fixtures/guard_vectors.json` was measured against, and editing them would have
  silently retired the conformance evidence.
* Wrote both Windows backends: allowlisted roots, inode-identity containment (Python) or
  canonicalised component containment (Rust), reparse-point refusal, reserved DOS device
  names, size bounds, typed confirmation checked last, device targets refused outright.
* Fixed `carve::relative_label`, which used `Path::is_absolute` — false for `/a/b` on
  Windows — so a POSIX host path could reach `run.image_path` verbatim.
* Fixed `ImageFile::open_read_only`, which opened before checking `is_file()` and so
  reported `DEVICE_IO` "access denied" for a directory on Windows.
* Fixed `.exe` suffixes in the two Python suites, `_FIXTURE_MODULES` for the new package
  layout, and made the telemetry rate-floor clause calibrate the host's sleep granularity
  and skip loudly rather than report a red that is about the Windows scheduler.
* Docs: `docs/architecture.md` D7, a Windows section in `README.md`,
  `tests/test_guard_windows.py` (24 clauses), and loud platform skips on the two POSIX
  guard suites naming what they are not covering.

Result on Windows: 484 cargo tests pass, 0 fail. The fixture builds and reproduces the
committed macOS digests exactly — `d85612b2…` and `1808494e…` — which turns the
cross-platform half of rule 6 from an inference into a measurement.

**Unsure.** Three things, and the first is the one that matters.

1. **The Windows guard is not TOCTOU-hardened and cannot be.** No `dir_fd`, no
   `O_NOFOLLOW`, and the Rust identity primitives are unstable. It resolves, checks, opens,
   re-checks. I put the limitation in every allow's `detail`, in D7 and in the README
   rather than anywhere it could be missed, but it is a genuinely weaker guard than the
   POSIX one and nobody should run a destructive target through it on the assumption that
   it is equivalent. The demo remains macOS/Linux.
2. **`DENY_MULTIPLE_HARDLINKS` is unreachable on Windows** — `st_nlink` is always 1 — so a
   hardlink from outside a root into it is not detected there. Published, not fixed.
3. **`FORBIDDEN_UNDER` excludes `USERS` by necessity**, because every Windows checkout
   lives under `C:\Users\<name>`. I believe refusing `C:\Users` itself and the profile
   directory itself is the right line, but it is a judgement call about a security
   constant and it deserves a second reader.

Not done and not claimed: no Windows CI runs any of this, so it holds on one machine. The
POSIX suites were not re-run on macOS after the move — the files are byte-identical and the
selector is a `cfg`, so I expect no change, but "expect" is the accurate word until someone
on a Mac runs them.
