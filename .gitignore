# build
target/
dist/
node_modules/
ui/src-tauri/target/

# python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# fixtures are generated from a seed and are never committed
fixtures/*.img
fixtures/*.raw
out/

# design sources — 21 MB of PDF and HTML that every clone would otherwise download.
# The tokens are already extracted into ui/tokens.css, so the build does not need them.
# Kept alongside the repo at ../design-source/, referenced in docs/frontend_contract.md.
*Design System.pdf
*Design System.html
design-source/
